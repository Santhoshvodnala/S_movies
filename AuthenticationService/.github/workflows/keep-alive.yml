name: Keep Render Awake

on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Services
        run: |
          curl https://s-movies-authenticationservice.onrender.com
          curl https://s-movies-nlo8.onrender.com